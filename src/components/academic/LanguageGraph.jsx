// import { LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer } from "recharts";

const LanguageGraph = () => {
  return (
    <div className='bg-white p-6 rounded-lg shadow-lg w-96'>
      grafico de idioma
    </div>
    // <ResponsiveContainer width="100%" height={300}>
    //   <LineChart data={data}>
    //     <XAxis dataKey="name" />
    //     <YAxis ticks={[0,1,2,3,4,5]} domain={[0,5]} />
    //     <Tooltip content={customTooltip} />
    //     <Line type="monotone" dataKey="Español" stroke="#22c55e" strokeWidth={3} />
    //     <Line type="monotone" dataKey="Ingles" stroke="#f59e0b" strokeWidth={3} />
    //     <Line type="monotone" dataKey="Alemán" stroke="#60a5fa" strokeWidth={3} />
    //   </LineChart>
    // </ResponsiveContainer>
  );
}

export default LanguageGraph;